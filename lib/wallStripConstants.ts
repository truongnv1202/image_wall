/** Khung hiển thị (logic scale theo viewport, max Full HD). */
export const STAGE_W = 1920;
export const STAGE_H = 1080;

/** Ô ảnh trên dải (3:4, 54×72px), tĩnh — không marquee. */
export const STRIP_TILE_W = 54;
export const STRIP_TILE_H = 72;

/** Popup ảnh mới (giới hạn tối đa). */
export const HERO_MAX_W = 780;
export const HERO_MAX_H = 1040;

export const HERO_POPUP_MS = 3000;
export const HERO_FLY_MS = 1200;

/** Khoảng cách giữa các ô (px). */
export const STRIP_GAP_PX = 2;

/** Xáo trộn thứ tự ảnh trên lưới (chỉ hiển thị), mặc định mỗi 1 phút. */
export const GRID_SHUFFLE_INTERVAL_MS = 60_000;
